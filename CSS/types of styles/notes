user-agent the browser deafult one style sheet
auther style- which writte in head tag using style element
user style who visit page

The "user-agent" is the browser. The "user" is the site visitor. The "author" is you, the developer.