Non-blocking I/O: JavaScript uses an event-driven, non-blocking I/O model.
 The event loop allows JavaScript to handle asynchronous operations 
 (e.g., network requests, file I/O) efficiently without blocking the main thread.